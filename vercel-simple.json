{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "bun install",
  "regions": ["iad1"],
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
EOF  
cd /home/project && cd boles-smart-shop && cat > vercel-simple.json << 'EOF'
{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "bun install",
  "regions": ["iad1"],
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
